# UI Change — Classic Black & White V1

Selected concept: User #1 + Admin #1.

Only visual presentation was changed. Existing JavaScript, API routes, models, Phase 1/2/3 feature modules, timing logic, authentication, live logic and quiz behavior were not intentionally changed.

New dedicated UI file:
- client/ui-classic-blackwhite-v1.css

The stylesheet is loaded after client/style.css in index.html and admin.html so it overrides the existing visual theme without replacing feature code.
